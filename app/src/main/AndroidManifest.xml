<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network access for loading Pokémon sprites from PokeAPI CDN -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Step counting (ACTIVITY_RECOGNITION is required on API 29+) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Heart rate monitoring -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- Required for watch faces -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- This is a Wear OS app -->
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- ── Watch Face Service ── -->
        <service
            android:name=".PokemonWatchFaceService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/preview" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview_circular"
                android:resource="@drawable/preview" />
        </service>

        <!-- ── Passive Health Data Listener ── -->
        <!-- Receives background step + HR updates from Health Services -->
        <service
            android:name=".health.PassiveDataService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.services.client.PASSIVE_LISTENER" />
            </intent-filter>
        </service>

        <!-- ── On-Watch Pokémon Picker ── -->
        <!-- Launched by the user to choose their starter -->
        <activity
            android:name=".ui.PokemonPickerActivity"
            android:exported="true"
            android:label="Choose Your Starter"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
